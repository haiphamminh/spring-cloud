spring:
  cloud:
    config:
      name: book-service
      discovery:
        service-id: config
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/